[pytest]
addopts = -ra --strict-markers
markers =
    smoke: P0 visitor journeys and content checks
    functional: public content and interactions
    accessibility: accessibility signal checks
    mobile: configured narrow viewport
